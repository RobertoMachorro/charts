apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "browsepvc.fullname" . }}-test-connection"
  labels:
    {{- include "browsepvc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "browsepvc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
