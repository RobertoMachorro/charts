apiVersion: v1
kind: Service
metadata:
  name: {{ include "browsepvc.fullname" . }}
  labels:
    {{- include "browsepvc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "browsepvc.selectorLabels" . | nindent 4 }}
