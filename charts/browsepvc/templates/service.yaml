apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  selector:
    workloadselector: {{ .Release.Name }}
  type: NodePort
  sessionAffinity: None
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
      targetPort: {{ .Values.image.containerPort }}
